{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn_update_admin_song">{{ button_label|default('Update') }}</button>

{{ form_end(form) }}

{# <form method="post" action="{{ path('product_edit', {'id': product.id}) }}">
    {{ form_widget(form) }}

    <button type="submit">Update</button>
</form> #}